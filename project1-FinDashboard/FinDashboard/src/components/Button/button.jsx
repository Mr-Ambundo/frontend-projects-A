import React from "react";

export default avatar  = (props) => {

    // code for setting the rendering and the shape and size of the buttons goes here
    //same with the props for each type of icon to be used through out the whole page.
    return(
        <>

        </>
    )
}